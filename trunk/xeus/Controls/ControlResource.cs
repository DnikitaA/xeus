using System.Windows ;
using System.Windows.Controls ;
using xeus.Core ;

namespace xeus.Controls
{
	partial class ControlResource
	{
		/*
		protected virtual void OnDeleteClicked( object sender, RoutedEventArgs e )
		{
			Button button = sender as Button ;

			if ( button != null )
			{
				RosterItem rosterItem = button.DataContext as RosterItem ;

				if ( rosterItem != null )
				{
					Client.Instance.Roster.DeleteRosterItem( rosterItem );
					e.Handled = true ;
				}
			}
		}*/
	}
}